%!TEX program = lualatex
\documentclass[11pt,letterpaper]{article}

% ── Font ──────────────────────────────────────────────────────────────────────
\usepackage{fontspec}
\setmainfont{Carlito}[
  UprightFont    = Carlito-Regular,
  BoldFont       = Carlito-Bold,
  ItalicFont     = Carlito-Italic,
  BoldItalicFont = Carlito-BoldItalic,
]

% ── Colors ────────────────────────────────────────────────────────────────────
\usepackage[usenames,dvipsnames]{xcolor}
\definecolor{dark-grey}{gray}{0.3}
\definecolor{text-grey}{gray}{0.08}

% ── Layout ────────────────────────────────────────────────────────────────────
\usepackage[letterpaper, left=0.5in, right=0.5in, top=0.5in, bottom=0.5in]{geometry}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{microtype}
\usepackage{tabularx}

% ── Page style ────────────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% ── Links ─────────────────────────────────────────────────────────────────────
\usepackage[hidelinks,unicode]{hyperref}
\usepackage{fontawesome5}

% ── Section headings ─────────────────────────────────────────────────────────
% Match Word: bold uppercase text, then thin black rule below, compact spacing
\titleformat{\section}{\bfseries\raggedright}{}{0em}{\MakeUppercase}[\vspace{1pt}\titlerule\vspace{-4pt}]
\titlespacing*{\section}{0pt}{6pt}{3pt}

% ── Resume macros ─────────────────────────────────────────────────────────────

% Two-row entry: {Name}{Right-top}{Subtitle}{Right-bottom}
\newcommand{\resumeSubheading}[4]{%
  \item
  \begin{tabular*}{\linewidth}[t]{l@{\extracolsep{\fill}}r}
    \textbf{#1} & \small #2\\
    \textit{\small #3} & \textit{\small #4}\\
  \end{tabular*}\vspace{-2pt}%
}

% Sub-heading row: {Left}{Right}
\newcommand{\resumeSubSubheading}[2]{%
  \item
  \begin{tabular*}{\linewidth}{l@{\extracolsep{\fill}}r}
    \textit{\small #1} & \textit{\small #2}\\
  \end{tabular*}\vspace{-2pt}%
}

% One-row entry: {Left}{Right}
\newcommand{\resumeProjectHeading}[2]{%
  \item
  \begin{tabular*}{\linewidth}{l@{\extracolsep{\fill}}r}
    \small#1 & {\small #2}\\
  \end{tabular*}\vspace{-2pt}%
}

% Bullet item
\newcommand{\resumeItem}[1]{\item\small{#1}}

% Lists — match Word: minimal left margin, tight spacing
\newcommand{\resumeSubHeadingListStart}{%
  \begin{itemize}[leftmargin=0in, label={}, itemsep=0pt, parsep=0pt, topsep=0pt]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}

\newcommand{\resumeItemListStart}{%
  \begin{itemize}[leftmargin=0.25in, itemsep=0pt, parsep=0pt, topsep=0pt, partopsep=0pt]}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-2pt}}

\renewcommand\labelitemii{$\vcenter{\hbox{\tiny$\bullet$}}$}

% ── Overflow guards ───────────────────────────────────────────────────────────
\setlength{\emergencystretch}{3em}
\setlength{\tabcolsep}{0in}
\raggedbottom
\raggedright

\color{text-grey}

% ──────────────────────────────────────────────────────────────────────────────
\begin{document}
\pagestyle{empty}

\input{sections/header.tex}

\section{Education}
\input{sections/education.tex}

\section{Experience}
\input{sections/experience.tex}

\section{Publications}
\input{sections/publications.tex}

\section{Volunteering}
\input{sections/volunteering.tex}

\section{Skills}
\input{sections/skills.tex}

\end{document}
